# Backend URL — set this to your Render service URL in Vercel's environment variables.
# Leave empty for local development (Vite proxy handles /auth and /api).
VITE_API_URL=https://your-render-service.onrender.com
