{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://lmha-be.vercel.app/api/:path*" },
    { "source": "/auth/:path*", "destination": "https://lmha-be.vercel.app/auth/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
